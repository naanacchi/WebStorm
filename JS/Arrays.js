let labels = [12.0,8,47,9.32]
alert(`drugi element tablicy to  ${labels[1]} `);
alert(`trzeci element tablicy to ${labels.length} `);

labels.push (7);
alert('drugi element tablicy' +labels[1])
alert('dlugosc tablicy to' +labels.length)

labels[1]=7
alert(labels)

